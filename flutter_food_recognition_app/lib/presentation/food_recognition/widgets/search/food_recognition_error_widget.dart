import 'package:flutter/material.dart';

class FoodRecognitionErrorWidget extends StatelessWidget {
  const FoodRecognitionErrorWidget({super.key});

  @override
  Widget build(BuildContext context) => const Center(
        child: Text('Erro no resultado'),
      );
}
